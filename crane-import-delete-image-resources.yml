- name: find image resources
  find:
    path: "{{ playbook_dir }}/output/{{ item }}"
    recurse: true
    patterns: 'Image.*yaml'
    use_regex: true
  register: image_resources

- name: Delete image resources
  file:
    state: absent
    path: "{{ resource.path }}"
  with_items: "{{ image_resources.files }}"
  loop_control:
    loop_var: "resource"
    label: "{{ resource.path }}"
